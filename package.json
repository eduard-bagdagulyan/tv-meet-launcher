{
  "name": "tv-meet-launcher",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Eduard Bagdagulyan",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "install-deps": "npm install",
    "install:chromium": "npx playwright install chromium",
    "setup": "npm run install-deps && npm run install:chromium",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "playwright": "^1.54.2"
  }
}
